apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "vault-service-account.fullname" . }}-test-connection"
  labels:
    {{- include "vault-service-account.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "vault-service-account.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
